{
  "name": "johnslipper-website",
  "version": "1.0.0",
  "scripts": {
    "production": "JEKYLL_ENV=production bundle exec jekyll build --destination _dist",
    "serve": "bundle exec jekyll serve"
  }
}
